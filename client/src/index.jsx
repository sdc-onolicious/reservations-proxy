import React from 'react';
import ReactDOM from 'react-dom';

const OldApp = () => (
  <div>
    <h1>this is the first thing</h1>
  </div>
);

ReactDOM.render(<OldApp />, document.getElementById('reviews'));
